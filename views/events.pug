extends ./layout

block main
  ul
    for event in events
      li
        a(href=`/event/${event.id}`)= event.name
        ul
          for attendee in event.attendees
            li= attendee.fullName
